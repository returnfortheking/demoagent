# HiSpark AI Agent — 系统设计文档

> **版本**: 2.0
> **日期**: 2026-02-28
> **状态**: 待验收

---

## 1. 项目背景与目标

### 1.1 为什么做这个项目

**目标 A — 工作价值**

HiSpark Studio for VS Code 是面向海思芯片开发者的集成开发环境，覆盖代码编写、编译、烧录、调试全流程。本项目为其注入 AI 能力，让开发者通过自然语言完成开发操作，并可随时查阅官方文档。

**目标 B — 技术学习**

通过一个真实项目，**演进式**地经历 LangChain 全技术栈的发展历史，覆盖大模型应用开发大厂面试中的全部高频考点（Q1–Q16），形成可讲述的实战经验。

### 1.2 项目宗旨

> 以真实工作需求为驱动，在每一次"旧方案 → 升级方案"的循环中，亲手实践 LangChain 演进史上的每个关键技术节点。**问题不在设计阶段预设，通过充分的测试和评估自然发现。**

---

## 2. 功能需求

### 2.1 核心功能

| ID | 功能模块 | 描述 |
|----|---------|------|
| F1 | 自然语言操控 | 通过对话控制 HiSpark 的编译、烧录、SDK下载、栈/镜像分析等操作 |
| F2 | 知识问答 | 基于官方文档的 RAG 问答，文档不足时自动联网补充 |
| F3 | 自定义知识库 | 用户可上传文档（PDF/MD/TXT），系统自动索引后参与检索 |
| F4 | 操作安全确认 | 不可逆操作（如烧录）执行前通过 Human-in-the-loop 确认 |
| F5 | 可观测性 | 全链路 Tracing（LangSmith），RAG 质量自动评估 |
| F6 | MCP 集成 | Agent 作为 MCP Client 调用工具；HiSpark Extension 作为 MCP Server |

### 2.2 非功能需求

- **可测试性**: 每个模块有独立单元测试，TDD 开发，每个版本有集成测试
- **可观测性**: v0.3 起接入 LangSmith，所有版本的 RAG 质量可量化对比
- **可移植性**: Python Backend 可独立运行（Gradio UI），不强依赖 VS Code

---

## 3. 系统架构

### 3.1 总体架构

```
┌─────────────────────── VS Code ──────────────────────────────┐
│                                                               │
│  ┌──── HiSpark AI Agent 扩展 (TypeScript) ────────────────┐  │
│  │                                                         │  │
│  │   Webview Chat Panel                                   │  │
│  │        ↕ postMessage                                   │  │
│  │   Extension Host                                       │  │
│  │   ├── HTTP Client  ──────────────────→ Python Backend  │  │
│  │   └── Command Executor                                 │  │
│  │        └── vscode.commands.executeCommand(...)         │  │
│  │                  ↓                                     │  │
│  │   HiSpark Studio 扩展（用户主导开发）                  │  │
│  │   ├── hispark-studio.build / rebuild / clean / flash   │  │
│  │   ├── hispark-studio.stackAnalysis / imageAnalysis     │  │
│  │   └── MCP Server（v0.8+）                              │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
                            ↕ HTTP (REST + SSE)
┌──────────────── Python Backend ───────────────────────────────┐
│                                                               │
│   FastAPI Server                                             │
│   ├── POST /chat          (JSON 非流式，v0.1 当前实现)        │
│   ├── POST /chat/stream   (SSE 流式，v0.2 规划)             │
│   ├── POST /kb/upload     (上传文档，v0.4 规划)             │
│   ├── GET  /kb/list       (v0.4 规划)                       │
│   └── DELETE /kb/{id}    (v0.4 规划)                        │
│                                                               │
│   LangGraph Agent (v0.5+)                                    │
│   ├── State: AgentState                                      │
│   ├── Node: agent / tools / hitl_confirm                     │
│   └── Edge: conditional routing                              │
│                                                               │
│   Tools                                                      │
│   ├── hispark_executor    (v0.1-v0.7: 返回结构化命令)        │
│   │   hispark_mcp_client  (v0.8+: 通过 MCP 协议调用)        │
│   ├── doc_retriever       (BM25 + BGE + Reranker)           │
│   ├── web_searcher        (v0.5+: 联网搜索兜底，需Tool机制)   │
│   └── kb_manager          (自定义知识库)                     │
│                                                               │
│   LLM: 智谱 GLM (OpenAI-compatible)                         │
│   向量库: Chroma + BAAI/bge-small-zh-v1.5                   │
│   Tracing: LangSmith（环境变量开关）                         │
└───────────────────────────────────────────────────────────────┘
```

### 3.2 关键设计决策

**决策 1：Python 是大脑，TypeScript 是手**

Agent 所有推理在 Python 中完成。执行 HiSpark 命令时，返回结构化动作对象：

```json
{
  "type": "action",
  "command": "hispark-studio.build",
  "args": {},
  "requires_confirmation": false,
  "description": "编译项目"
}
```

TypeScript Extension Host 接收后调用 `vscode.commands.executeCommand()`。

**决策 2：危险操作走 HITL**

烧录等不可逆操作，通过 LangGraph Checkpoint 暂停，用户确认后恢复执行。

**决策 3：MCP 双角色（v0.8）**

- Python Agent 作为 **MCP Client**：先接入社区 MCP Server（filesystem/web-search），体验客户端协议细节
- HiSpark Extension 实现 **MCP Server**：将 hispark-studio.build 等命令暴露为 MCP Tools，Agent 通过 MCP 协议发现并调用

**决策 4：双轨并行开发**

Extension 从 v0.1 起同步开发（最简骨架），Python 和 TypeScript 各自演进，每个版本包含集成测试验证两侧协作正确。

---

## 4. 演进路线图

### 4.1 版本总览

每个版本包含两个轨道（Python Backend + TypeScript Extension）和三层测试（Python 单元、TS 单元、集成测试）。

> **评估数据集前置（按当前实现）**：v0.1 基线固定为 `operation_qa` 20 条；`document_qa` 从 v0.3 起自动生成。后续版本复用同一 `operation_qa` 做横向对比。

```
版本    Python Backend 演进            Extension 演进                    覆盖面经    里程碑
─────────────────────────────────────────────────────────────────────────────────────────
v0.1    旧版 LangChain + Gradio        骨架 + Webview + 命令执行器        Q2 Q3 Q14   ★ MVP
        LLMChain + RetrievalQA         (知识问答 + 自然语言操控 可用)
v0.2    LCEL + SSE流式                 Webview 流式输出                   Q1
        Prompt版本管理 + API契约门禁
v0.3    LangSmith + LLM-as-a-Judge    对话历史 + 来源展示                Q6 Q7
        性能基线 + Docker
v0.4    进阶 RAG + RAGAS + K8s        知识库文件上传 UI                  Q8 Q9 Q10
v0.5    LangGraph Agentic RAG         工具调用过程可视化                 Q4 Q11 Q12
        + web_searcher + 降级回滚
v0.6    Checkpoint + HITL             确认对话框                         Q5 Q15
v0.7    Multi-Agent Supervisor        多 Agent 状态展示                  Q13
v0.8    MCP Client + MCP Server       HiSpark MCP Server                Q16
v1.0    发布准备                       打包 .vsix                        全覆盖      🚀 发布
```

**MVP（v0.1）说明**：v0.1 完成后，用户可以在 VS Code 中：
1. 用自然语言触发 HiSpark 命令（编译/烧录/SDK下载等）——Extension 命令执行器负责调用 `executeCommand`
2. 用自然语言查询 HiSpark 官方文档——`RetrievalQA` 负责检索回答

后续版本在此基础上持续提升质量（流式体验、检索精度、可观测性、安全确认、架构演进），而非增加新的核心功能。

### 4.2 各版本详细规格

---

#### v0.1 — 旧版 LangChain + Extension 骨架

**Python Backend 功能**

- `LLMChain`：解析用户意图，识别 HiSpark 命令名和参数
- `RecursiveCharacterTextSplitter`：切分 HiSpark 官方文档
- `Chroma`：向量存储（内存模式）
- `RetrievalQA`：文档问答链
- `thread_id`：贯穿请求的会话标识（v0.1 仅透传，记忆能力在后续版本增强）
- Gradio Web UI：独立于 Extension 的调试界面

**TypeScript Extension 功能**

- `package.json`：扩展清单，注册 `hispark-ai-agent.openChat` 命令
- `extension.ts`：`activate()` 入口，创建 WebviewPanel
- Webview HTML：最简聊天框（输入框 + 发送按钮 + 消息列表）
- Extension Host ↔ Webview：`postMessage` 消息通道
- Extension Host ↔ Backend：`fetch` 调用 `POST /chat`（非流式）
- **命令执行器**：解析 Backend 返回的结构化动作，调用 `vscode.commands.executeCommand()`

**接口契约（Python ↔ TypeScript）**

```
POST /chat
Request:  { "message": string, "thread_id": string }
Response: {
  "type": "answer" | "action",
  // type=answer：知识问答响应
  "answer"?: string,
  "sources"?: string[],
  // type=action：命令执行响应
  "command"?: string,          // e.g. "hispark-studio.build"
  "args"?: object,             // e.g. { "mode": "release" }
  "requires_confirmation"?: boolean,
  "description"?: string       // 向用户展示的操作说明
}
```

Extension Host 收到 `type=action` 时，调用 `vscode.commands.executeCommand(command, args)`；收到 `type=answer` 时，在 Webview 展示文本。

**测试规格**

*Python 单元测试（pytest）*
```
backend/tests/unit/
├── test_intent_classifier.py  # LLMChain 意图分类（action/answer + 异常分支）
├── test_qa_chain.py           # RetrievalQA 调用逻辑（mock 为主）
└── test_api.py                # /chat 与 /health 契约

backend/tests/integration/
└── test_llm_real.py           # 真实 LLM/Embedding 连通与 RAG 关键词断言
```

*TypeScript 单元测试（Mocha）*
```
extension/src/test/suite/
├── chatPanel.test.ts          # Webview HTML 结构与 postMessage 逻辑
├── apiClient.test.ts          # HTTP Client URL 拼接与响应解析
└── commandExecutor.test.ts    # action/confirmation/answer 分支
```

*集成测试*
```
tests/e2e/
└── test_e2e_v01.py
    # 启动 FastAPI 测试服务器
    # GET /health 返回 200 + {"status":"ok"}
    # 发送"帮我编译项目"，验证 type=action，command=hispark-studio.build
    # 发送"烧录固件"，验证 requires_confirmation=true
    # 缺失 message 字段返回 422
```

*基线评估（人工标注，非 RAGAS）*
```
evaluation/
└── v01_baseline.py
    # 使用 operation_qa（20条）数据集（docs/evaluation/dataset.json）
    # 比较命令识别结果，记录 command_match_rate
    # 产出：docs/evaluation/results/v0.1-baseline.json（供后续版本对比）
```

---

#### v0.2 — LCEL 重构 + 流式输出

**Python Backend**

- `LLMChain` → `prompt | llm | StrOutputParser()`
- `RetrievalQA` → LCEL RAG Chain（`retriever | format_docs | prompt | llm`）
- 新增会话记忆能力：`RunnableWithMessageHistory`
- API 新增 SSE 流式端点：`POST /chat/stream`（`text/event-stream`）
- 保留 `POST /chat`（非流式，向后兼容）
- **Prompt 版本管理**：所有 Prompt 提取到 `src/prompts/` 目录，文件名含版本号（如 `intent_v1.py`），链从 prompts/ 导入，不含内联字符串
- **API 契约门禁**（F21）：新增集成测试锁定 `/chat` 与 `/chat/stream` 的字段名、类型、状态码，防止接口漂移

**TypeScript Extension**

- Webview 接收 SSE 流，逐 token 渲染（`EventSource` → `postMessage` → Webview）
- 消息显示增加"正在思考..."loading 状态

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_lcel_chain.py       # LCEL chain：invoke / stream / batch 均可用
├── test_lcel_rag.py         # LCEL RAG：检索结果正确注入 prompt
└── test_streaming.py        # SSE 端点：逐 chunk 输出，不等全文完成
```

*TypeScript 单元测试*
```
extension/tests/unit/
└── test_sse_handler.ts      # SSE 解析：chunk 按序到达，连接中断后重连
```

*集成测试*
```
tests/integration/
└── test_v02_streaming.py
    # 启动 FastAPI，通过 SSE 发送问题
    # 验证首个 token 在 2s 内到达（流式响应不等全文）
    # 验证完整响应与非流式结果语义一致
```

*回归测试*：运行 v0.1 所有测试，验证重构未破坏功能

---

#### v0.3 — LangSmith + 评估体系

**Python Backend**

- 配置 LangSmith Tracing（4 个环境变量）
- `@traceable` 标记核心函数
- 构建评估数据集：20 条 HiSpark QA 对（10 条操作类 + 10 条文档类）
- 实现两个评估器：Faithfulness（忠诚度）、Answer Relevancy（答案相关性）
- 运行 v0.1、v0.2 的历史评估，产出对比报告
- **性能与成本基线**（F26）：脚本运行 10 条查询，记录 p95 延迟与 token 消耗，落盘 JSON，供后续版本横向对比
- **Docker 部署流水线**（F27）：`backend/Dockerfile` + 根目录 `docker-compose.yml`，`docker compose up` 可启动完整服务，`/health` 返回 200

**TypeScript Extension**

- 对话历史持久化（Webview `setState`，面板重开后恢复）
- 消息中展示来源引用（文档名 + 段落）

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_faithfulness_evaluator.py
│   # 给定 context + answer，评估器能判断忠诚/幻觉
│   # 测试边界：答案完全来自 context → score 接近 1
│   # 测试边界：答案包含 context 中没有的信息 → score 较低
├── test_relevancy_evaluator.py
│   # 给定 question + answer，评估答案相关性
└── test_dataset.py
    # 评估数据集格式验证（每条有 question + reference_answer）
```

*TypeScript 单元测试*
```
extension/tests/unit/
└── test_history_persistence.ts  # setState/getState 跨会话恢复
```

*集成测试*
```
tests/integration/
└── test_v03_evaluation.py
    # 对评估数据集跑完整 RAG pipeline
    # 断言：Faithfulness 均值 > 评估基线（由测试结果确定，非预设）
    # 断言：Answer Relevancy 均值 > 评估基线
    # 产出：docs/evaluation/v03_report.json
```

---

#### v0.4 — 进阶 RAG（Chunking + 混合检索 + Reranker + RAGAS）

**Python Backend**

- 实现 4 种切块策略，通过配置切换：
  - 固定大小（`RecursiveCharacterTextSplitter`）
  - 滑动窗口（`chunk_overlap` 加大）
  - 语义切分（`SemanticChunker`）
  - 结构化切分（按 Markdown 标题层级）
- BM25 检索器（`rank_bm25`）
- BGE 向量检索器（`BAAI/bge-small-zh-v1.5`）
- RRF 融合算法
- BGE-Reranker Cross-Encoder 精排（`BAAI/bge-reranker-v2-m3`）
- RAGAS 4 指标评估（Context Recall/Precision, Faithfulness, Answer Relevancy）；评估结果落盘后加入 `gate-feature.sh` 门禁
- **K8s 编排**（F37）：`k8s/` 目录含 Deployment、Service、ConfigMap、HPA 清单；Chroma 数据通过 PersistentVolumeClaim 持久化；`kubectl apply -f k8s/` 可部署完整服务

**TypeScript Extension**

- 知识库管理面板：文件上传（`.md`/`.pdf`/`.txt`）、文档列表、删除
- 调用 `POST /kb/upload`、`GET /kb/list`、`DELETE /kb/{id}`

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_chunking_strategies.py
│   # 每种策略：给定相同文档，产出合理的 chunk 数量和大小
│   # 结构化切分：Markdown 标题边界不被截断
│   # 所有策略：chunk 之间无内容丢失（重组后与原文一致）
├── test_bm25_retriever.py
│   # 精确关键词查询命中率
│   # 同义词查询（BM25 预期不如向量检索）
├── test_hybrid_retriever.py
│   # RRF 融合：两路都有结果时，排名靠前的文档得分更高
│   # 边界：一路无结果时，融合结果等于另一路
└── test_reranker.py
    # Reranker 精排：query-doc 相关性高的排名靠前
    # 性能：top-5 精排耗时 < 2s
```

*TypeScript 单元测试*
```
extension/tests/unit/
└── test_kb_upload.ts  # 文件选择、上传进度、错误处理（大文件/不支持格式）
```

*集成测试*
```
tests/integration/
├── test_v04_retrieval_quality.py
│   # 对 operation_qa + document_qa 组合评估集，用 4 种切块策略 × 3 种检索策略分别跑
│   # 记录每种组合的 RAGAS 4 指标
│   # 产出：docs/evaluation/v04_strategy_comparison.json
│   # （不预设哪种策略最好，由测试结果说话）
└── test_v04_kb_upload.py
    # 上传文档后，能在 QA 中检索到其内容
    # 删除文档后，内容不再出现在检索结果中
```

---

#### v0.5 — LangGraph（State/Node/Edge + Agentic RAG + ReAct）

**Python Backend**

- `StateGraph` 重建 Agent：
  ```python
  class AgentState(TypedDict):
      messages: Annotated[list, operator.add]
      retrieved_docs: list[Document]
      iteration_count: int
  ```
- 条件边：有工具调用 → tools 节点；无 → END
- 检索作为 Tool，LLM 自主决定是否调用、调用几次
- 最大迭代次数限制，防止无限循环
- **web_searcher Tool**（首次引入）：文档检索不足时，LLM 自主决定是否联网搜索
- **降级与回滚策略**（F50）：per-node fallback——web_search 超时自动降级到 RAG 路径；LLM 异常时返回结构化错误；集成测试覆盖各降级路径

**TypeScript Extension**

- 展示 Agent 推理过程（Thought/Action/Observation 逐步显示）
- 区分"正在检索..."和"正在生成..."两种 loading 状态

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_agent_state.py
│   # State 合并：messages 追加（不覆盖）
│   # iteration_count 每轮递增
├── test_agent_routing.py
│   # 条件边：mock LLM 返回 tool_call → 路由到 tools
│   # 条件边：mock LLM 返回无 tool_call → 路由到 END
│   # 超过最大迭代 → 强制结束，不抛异常
└── test_agentic_rag.py
    # 简单问题：1次检索后回答
    # 复杂问题：允许多次检索（验证迭代能力）
    # 无关问题：不触发检索工具直接回答
```

*TypeScript 单元测试*
```
extension/tests/unit/
└── test_streaming_steps.ts  # ReAct 步骤流式渲染：每个 step 按序显示
```

*集成测试*
```
tests/integration/
└── test_v05_agentic_rag.py
    # 对比 v0.2（固定流程 RAG）vs v0.5（Agentic RAG）
    # 在相同 20 条 QA 对上跑 RAGAS，记录对比结果
    # 验证：需多步推理的问题，Agentic RAG 正确率 >= 固定流程
```

---

#### v0.6 — Checkpoint + Human-in-the-loop

**Python Backend**

- `MemorySaver` Checkpointer
- `interrupt_before=["confirm_node"]` 暂停危险操作
- `graph.update_state()` 注入确认结果
- `graph.invoke(None, config)` 断点恢复
- `thread_id` 区分不同用户会话

**TypeScript Extension**

- 接收 `requires_confirmation: true` 的响应时，弹出确认对话框
- 用户点击确认/取消 → 发送 `POST /chat/confirm`
- 显示"等待确认中..."状态

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_checkpoint.py
│   # 中断后 State 完整保存（messages/iteration_count 不丢失）
│   # 相同 thread_id 恢复后继续执行
│   # 不同 thread_id 互不影响（State 隔离）
└── test_hitl_flow.py
    # 危险命令 → 图暂停 → 注入 approved=True → 继续执行
    # 危险命令 → 图暂停 → 注入 approved=False → 操作取消
    # 非危险命令 → 图不暂停，直接执行
```

*TypeScript 单元测试*
```
extension/tests/unit/
└── test_confirmation_dialog.ts
    # 收到 requires_confirmation 响应 → 显示确认框
    # 点击取消 → 发送 confirmed=false
    # 点击确认 → 发送 confirmed=true
```

*集成测试*
```
tests/integration/
└── test_v06_hitl.py
    # 端到端：发送烧录指令 → 后端暂停 → 模拟用户确认 → 验证命令被执行
    # 端到端：发送烧录指令 → 后端暂停 → 模拟用户取消 → 验证命令未执行
    # 并发：两个 thread_id 同时等待确认，互不影响
```

---

#### v0.7 — Multi-Agent Supervisor

**Python Backend**

- Supervisor Agent：路由到 DevOps Agent 或 Knowledge Agent
- DevOps Agent：专门处理编译/烧录/SDK/分析类操作
- Knowledge Agent：专门处理文档检索和知识问答

**TypeScript Extension**

- 展示当前是哪个 Agent 在处理（"操作专家"/"知识专家"标签）

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_supervisor_routing.py
│   # 操作类请求 → 路由到 DevOps Agent
│   # 问答类请求 → 路由到 Knowledge Agent
│   # 模糊请求 → 测试实际路由行为（不预设）
└── test_subagent_independence.py
    # DevOps Agent 有且只有操作工具
    # Knowledge Agent 有且只有检索工具
```

*集成测试*
```
tests/integration/
└── test_v07_multiagent.py
    # 对比 v0.5（单 Agent）vs v0.7（Supervisor）
    # 在相同 20 条 QA 对上跑 RAGAS 和命令识别准确率
    # 记录结果（不预设哪个更好）
```

---

#### v0.8 — MCP 集成（双角色）

**阶段 A：Python Agent 作为 MCP Client**

接入现有社区 MCP Server，体验 Client 端协议：
- **filesystem MCP Server**：读取当前工作区的代码文件（用于代码相关问答）
- **web-search MCP Server**：联网搜索（替换当前的 web_searcher 工具）

通过 `langchain-mcp-adapters` 或直接实现 MCP Client 协议：
- 工具发现（`tools/list`）
- 工具调用（`tools/call`）
- 资源读取（`resources/read`）

**阶段 B：HiSpark Extension 作为 MCP Server**

在 HiSpark Studio Extension（TypeScript）中实现 MCP Server（stdio 传输）：
- 暴露 Tool：`hispark_build`、`hispark_flash`、`hispark_download_sdk`、`hispark_stack_analysis`、`hispark_image_analysis`
- 暴露 Resource：当前工程配置（`.config`、`toolChain.json`）
- Python Agent 通过 MCP 协议发现并调用这些工具（替换原有 `hispark_executor` 结构化命令方案）

**测试规格**

*Python 单元测试*
```
tests/unit/
├── test_mcp_client.py
│   # 工具发现：连接 mock MCP Server，返回正确的工具列表
│   # 工具调用：发送 tools/call，接收正确响应
│   # 资源读取：发送 resources/read，返回文件内容
│   # 协议细节：请求/响应 JSON-RPC 格式正确
└── test_mcp_vs_function_call.py
    # 对比：Function Call 工具需在代码里硬编码 schema
    # 对比：MCP 工具可运行时发现，不修改 Agent 代码
```

*TypeScript 单元测试*
```
extension/tests/unit/
└── test_mcp_server.ts
    # MCP Server 初始化：工具列表正确
    # hispark_build Tool：接收参数，调用 vscode.commands，返回结果
    # hispark_flash Tool：标记 requires_confirmation=true
    # 资源列表：返回当前工程配置文件路径
```

*集成测试*
```
tests/integration/
└── test_v08_mcp.py
    # Python Agent → MCP Client → HiSpark MCP Server → VS Code Command
    # 完整链路：自然语言 → Agent → MCP 调用 → 命令执行 → 结果返回
    # 对比：MCP 方案 vs v0.5 结构化命令方案的功能等价性
```

---

## 5. 测试架构

### 5.1 测试分层

```
┌────────────────────────────────────────────────────┐
│            集成测试（每个版本）                     │
│  Python Backend ↔ TypeScript Extension 端到端验证   │
├───────────────────┬────────────────────────────────┤
│  Python 集成测试  │  TypeScript 集成测试            │
│  多组件联合验证   │  Extension Host ↔ Webview       │
├───────────────────┼────────────────────────────────┤
│  Python 单元测试  │  TypeScript 单元测试            │
│  单个函数/类      │  单个 TS 模块                   │
└───────────────────┴────────────────────────────────┘
```

### 5.2 跨栈集成测试策略

Python 和 TypeScript 之间通过 HTTP API 通信，集成测试采用以下方式：

**API 契约测试（v0.2 F21 起接入）**：`/chat` 与 `/chat/stream` 的字段名、类型、状态码由集成测试自动守护。OpenAPI Spec（`docs/api-spec.yaml`）在 v1.0 F75 完善。

**E2E 测试**：启动真实 FastAPI 服务器，用 Python `httpx` 客户端模拟 Extension 行为，验证完整业务流程（不启动 VS Code 环境）。可在 Docker 容器内运行（subprocess 自洽）。

**VS Code Extension 测试**：使用 `@vscode/test-electron` 在真实 VS Code 进程中运行，测试 Webview 消息通道、命令执行、文件系统访问等 VS Code 特有行为。需要显示器，**不适合在 Docker 中运行**；本地直接执行，CI 环境使用 `xvfb-run`（v0.3 接入时处理）。

### 5.3 CI/CD 策略

| 阶段 | 门禁形式 | 说明 |
|------|---------|------|
| v0.1–v0.2 | 本地 commit-msg hook | `feat(Fxx):` 提交自动触发 `gate-feature.sh`（8 关） |
| v0.3+ | GitHub Actions | Docker 就绪后接入，`xvfb-run` 解决 Extension E2E 显示器问题 |

**Docker 与测试的边界**：Docker 用于服务部署和 backend E2E 环境，Extension E2E 始终在有显示器的宿主机或 CI runner 上运行，不进容器。

### 5.3 RAG 评估体系

**评估数据集（两类，来源和生命周期不同）**

```
docs/evaluation/dataset.json
├── operation_qa（20条，手工定义，项目启动前一次性创建）
│   └── 自然语言 → HiSpark 命令映射
│       覆盖命令: hispark-studio.build / rebuild / clean / flash /
│                stackAnalysis / imageAnalysis / stopBuild / portionOfBurn
└── document_qa（自动生成，首次生成于 v0.3）
    └── RAGAS TestsetGenerator 从官方使用指南自动生成
        source: HiSpark Studio for VS Code 使用指南.md（352KB）
        script: backend/scripts/generate_docqa.py
        优势: 与知识库同源、覆盖面由文档决定、文档更新后可重新生成
```

**评估方法演进**

| 版本 | 数据集 | 评估方法 | 产出 |
|------|--------|---------|------|
| v0.1 | operation_qa（20条） | 命令 ID 精确匹配 | `v01_baseline.json`（操作准确率基线） |
| v0.3 | operation_qa + 自动生成 document_qa | LLM-as-a-Judge（Faithfulness + Answer Relevancy） | `v03_llm_judge_report.json` |
| v0.4 | 同上 | RAGAS 全 4 指标（+ 策略对比矩阵） | `v04_strategy_comparison.json` |
| v0.5 | 同上 | RAGAS（Agentic vs Chain 对比） | `v05_agentic_vs_chain.json` |
| v0.7 | 同上 | RAGAS（Multi-Agent vs 单 Agent） | `v07_multiagent_vs_single.json` |

**原则：评估结果只记录事实，不在设计阶段预设结论。**

### 5.4 TDD 执行规范

```
1. 写测试          → 运行确认 FAILED（必须看到红灯）
2. 写最少实现      → 运行确认 PASSED
3. 重构            → 运行确认仍然 PASSED
4. 提交            → git commit（每个功能点独立提交）
```

---

## 6. 技术栈

### 6.1 Python Backend

| 组件 | 选型 |
|------|------|
| LLM | 智谱 GLM-4.7 / GLM-5（OpenAI-compatible） |
| LLM 框架 | LangChain 0.3.x + LangGraph 0.2.x |
| Web 框架 | FastAPI + uvicorn（SSE 支持） |
| 向量数据库 | Chroma |
| 嵌入模型 | BAAI/bge-small-zh-v1.5（本地，无 API 费用） |
| 关键词检索 | rank_bm25 |
| 精排模型 | BAAI/bge-reranker-v2-m3（本地） |
| RAG 评估 | RAGAS |
| 开发期 UI | Gradio |
| 测试 | pytest + pytest-asyncio + httpx |
| 依赖管理 | uv + pyproject.toml |
| MCP Client | mcp（官方 Python SDK）+ langchain-mcp-adapters |

### 6.2 TypeScript Extension

| 组件 | 选型 |
|------|------|
| 扩展框架 | VS Code Extension API |
| 语言 | TypeScript 5.x |
| HTTP Client | node-fetch（Extension Host 侧） |
| Webview UI | Vanilla JS + CSS（避免打包复杂性） |
| MCP Server | @modelcontextprotocol/sdk（v0.8+） |
| 测试 | @vscode/test-electron + Mocha |

### 6.3 LLM 配置

```python
# backend/src/config.py
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
import os

def get_llm(model: str = "glm-4-flash") -> ChatOpenAI:
    return ChatOpenAI(
        model=model,
        openai_api_key=os.environ["ZHIPU_API_KEY"],
        openai_api_base="https://open.bigmodel.cn/api/paas/v4/",
        temperature=0.1,
    )

def get_embeddings() -> OpenAIEmbeddings:
    # 可替换为本地 BGE，无 API 费用
    return OpenAIEmbeddings(
        model="embedding-3",
        openai_api_key=os.environ["ZHIPU_API_KEY"],
        openai_api_base="https://open.bigmodel.cn/api/paas/v4/",
    )
```

---

## 7. 项目目录结构

> 注：以下为目标目录结构，含已落地模块与规划项。

```
hispark-ai-agent/
│
├── docs/
│   ├── plans/
│   │   ├── 2026-02-28-design.md        ← 本文档
│   │   └── 2026-03-02-v0.1-plan.md     ← v0.1 TDD 实现计划（已落地）
│   ├── evaluation/                      ← 各版本评估报告（运行后生成）
│   └── adr/                             ← Architecture Decision Records（规划项）
│       └── 001-python-brain-ts-hands.md（规划项）
│
├── backend/
│   ├── src/
│   │   ├── config.py                    ← LLM/环境变量配置
│   │   ├── prompts/                     ← v0.2+ 版本化 Prompt 文件
│   │   ├── chains/                      ← v0.1-v0.2 LangChain 链
│   │   ├── rag/                         ← RAG 组件（loader/splitter/retriever）
│   │   ├── agents/                      ← v0.5+ LangGraph Agent
│   │   ├── tools/                       ← Agent 工具
│   │   ├── evaluation/                  ← RAGAS + LangSmith 评估
│   │   └── api/                         ← FastAPI 接口
│   ├── tests/
│   │   ├── unit/
│   │   └── integration/
│   ├── Dockerfile                       ← v0.3+
│   ├── pyproject.toml
│   └── .env.example
│
├── extension/
│   ├── src/
│   │   ├── extension.ts
│   │   ├── webview/
│   │   └── hispark/                     ← v0.8 MCP Server 实现
│   └── tests/
│       ├── unit/
│       └── integration/
│
├── docs/evaluation/
│   └── dataset.json                     ← 当前含 operation_qa 20 条（document_qa 预留）
│
├── knowledge_base/
│   ├── official/                        ← HiSpark 官方文档
│   └── custom/                          ← 用户上传文档
│
├── docker-compose.yml                   ← v0.3+
├── k8s/                                 ← v0.4+ K8s 编排清单
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── configmap.yaml
│   └── hpa.yaml
├── .gitignore
├── CLAUDE.md
└── README.md
```

---

## 8. 面经覆盖地图

| 面经题目 | 覆盖版本 | 项目中的具体体现 |
|---------|---------|----------------|
| Q1: LCEL | v0.1→v0.2 | 同一功能用旧版和 LCEL 两种写法实现，测试验证行为等价 |
| Q2: Chain vs Agent | v0.2→v0.5 | LCEL Chain 和 LangGraph Agent 处理相同任务，RAGAS 对比 |
| Q3: RAG 7步流程 | v0.1 | 首次完整实现，单元测试覆盖每个步骤 |
| Q4: State/Node/Edge | v0.5 | LangGraph 核心结构，单元测试验证每个节点和边的行为 |
| Q5: HITL | v0.6 | 烧录确认流程，集成测试验证暂停/恢复/取消三条路径 |
| Q6: LangSmith Tracing | v0.3 | Trace 配置，单元测试验证 @traceable 函数被正确追踪 |
| Q7: LLM-as-a-Judge | v0.3 | Faithfulness/Relevancy 评估器，单元测试验证评分边界 |
| Q8: Chunking | v0.1→v0.4 | v0.1 基础切块 → v0.4 四种策略，RAGAS 对比结果 |
| Q9: 召回优化 | v0.4 | BM25+向量+Reranker 两阶段，单元测试验证每个组件 |
| Q10: RAGAS | v0.3→v0.4 | 评估体系建立，跨版本指标对比 |
| Q11: Agentic RAG | v0.5 | 检索作为 Tool，单元测试验证迭代检索行为 |
| Q12: ReAct | v0.5 | Thought-Action-Observation 循环，单元测试验证路由逻辑 |
| Q13: Multi-Agent | v0.7 | Supervisor 路由，集成测试验证不同类型请求的路由结果 |
| Q14: Vector DB | v0.1 | Chroma 使用，单元测试验证 CRUD 操作 |
| Q15: Checkpoint | v0.6 | thread_id 隔离，单元测试验证多会话 State 独立性 |
| Q16: MCP | v0.8 | Client 端：工具发现/调用；Server 端：HiSpark 工具暴露 |

---

## 9. 开发原则

- **测试驱动**: 先写测试看红灯，再写实现看绿灯
- **结论由测试产生**: 评估结果记录事实，不在设计阶段预设
- **YAGNI**: 每个版本只实现当前版本需要的功能
- **频繁提交**: 每个功能点独立 commit，保持 git log 可读
- **Changelog 事后记录**: 版本 changelog 在开发完成后根据实际情况填写，不预写

---

## 10. 开始条件

在开始 v0.1 开发前，需要确认：

- [ ] 智谱 API Key 已获取（`bigmodel.cn` → 个人中心）
- [ ] Python 3.11+ 已安装
- [ ] Node.js 18+ 已安装（Extension 开发）
- [ ] 本文档已通过验收
